#include "mini_uart.h"
#include "cpio.h"
#include "string_utils.h"

void cpio_ls()
{
    // /* 1. Get the address of cpio_address. */
    // char *addr = (char *)QEMU_CPIO_ADDR;

    // /* 2. Use while loop to check the pathname of the file is "TRAILER!!!" or not. */
    // while (my_strcmp((addr + sizeof(struct cpio_newc_header)), "TRAILER!!!") != 0) {

    //     /* 3. */
    //     struct cpio_newc_header *header = (struct cpio_newc_header *)addr;

    // }

}